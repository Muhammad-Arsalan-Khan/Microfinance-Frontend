// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#005EB8',
    },
    success: {
      main: '#8BC441',
    },
    background: {
      default: '#F0F0F0',
    },
  },
});

export default theme;
